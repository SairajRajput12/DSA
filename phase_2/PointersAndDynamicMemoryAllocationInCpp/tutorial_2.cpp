#include<bits/stdc++.h>
using namespace std;
int main(){
     // enter the project name
     /*
     cout << "arrays and addresses" << endl; 
     int a[10] = {0,1,2}; 
     cout <<"Address of the first memory block is :" << a << endl;
     // using & operator: 
     // & -----> Address of. 
     cout << "Address of the  first memory block is :" << &a[0] << endl;
     cout << "4th :" << *a << endl;// prints the value at first location. 
     cout << "5th :" << *a + 1 << endl; 
     cout << "6th :" << *a + 2 << endl;
     cout << "7th :" << *(a) + 1 << endl; 
     cout << "8th :" << *(a + 1) << endl;
     // * ----> value at and & ----> Address of. 
     cout << "9th :" << a[2] <<endl; 
     cout << "10th :" << *(a + 2) <<endl; 
     // arr[i] = *(arr + i); 
     // i[a] = *(i + a) 
     cout << "1[a] :" << 1[a] << endl;
     // or: 
     cout << "*(1 + a) :" << *(1 + a) << endl;


     // Difference between arrays and pointer. 
    //  int *p = &a[0]; 
     int *p = a;  
     cout << "*p :" << *p << endl;
     cout << "&p :" << &p << endl; 
     */ 

    //  int arr[10] = {23,122,41,67}; 
    //  int temp[10]; 
    //  cout << sizeof(temp) << endl; // 40 
    //  int *ptr = temp; 
    //  cout << sizeof(ptr) << endl; // 8 
    //  cout << sizeof(*ptr) << endl; // 4 
    //  cout << sizeof(&ptr) << endl; // 8
     // size -----------> no of blocks * 4. 
     //         |-------> 8 bytes 
    //  int a[20] = {1,2,3,4,5}; 
    //  cout << &a[0] << endl; 
    //  cout << &a << endl; 
    //  cout << a << endl;

    //  int *p = &a[0]; 
    //  cout << p << endl; 
    //  cout << *p << endl; 
    //  cout << &p << endl;
     // symbol table content can't be changed. 
    //  cout << a[1] << endl;
    //  int *p = a ; 
    //  cout << *p << endl; 
    //  p = p + 1; 
    //  cout << *p << endl;
     // symbol table ke content ko change nhi kar skte
     
  return 0;
}